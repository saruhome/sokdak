# sokdak

Expo + React Native + TypeScript 기반 프로젝트입니다.

## 시작하기

```
npm install
npm run start
```

## 스크립트

- `npm run android` — Android 실행
- `npm run ios` — iOS 실행 (macOS 필요)
- `npm run web` — 웹 미리보기
