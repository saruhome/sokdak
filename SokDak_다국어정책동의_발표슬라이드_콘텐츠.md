## Cover

# SokDak 다국어 커뮤니티 정책 동의 시스템

한국 거주 외국인 학습자를 위한 안전한 참여 경험과 증빙 가능한 동의 원장

비공개 베타 출시 준비 · Android 우선

## Slide 1

# 커뮤니티 참여의 핵심 과제

- 1차 대상: 영어·일본어·스페인어·베트남어 원어민
- 가입 후 커뮤니티 탭에서 정책 확인은 필수
- 읽기는 열어두고, 게시·댓글은 최신 정책 동의 후에만 허용
- 개인 신원 노출을 최소화하면서 운영 가능한 안전장치 필요

## Slide 2

# Timestamp만으로는 증명할 수 없습니다

- 기존 문제: 정책 동의 열이 `profiles`에서 `account_settings`로 이동
- 단일 동의 시각은 정책 개정과 표시 언어를 구분하지 못함
- 클라이언트가 임의로 캐시를 변경하면 동의 증빙이 약해짐
- 해결 원칙: 동의 이력은 서버가 작성하고, 과거 기록은 덮어쓰지 않음

## Slide 3

# 동의를 하나의 감사 가능한 원장으로

- `community_policy_versions`: 정책 키·버전·효력 시각·한국어 원문 해시
- `community_policy_locales`: 언어별 공개 URL·표시 원문 해시·검토 상태
- `community_policy_consents`: 사용자·정책 버전·표시 언어·서버 수락 시각
- 사용자와 정책 버전 조합은 1회만 기록, 새 정책은 새 행으로 보존

## Slide 4

# 서버가 최신 동의만 인정합니다

- `accept_current_community_policy` RPC가 로그인·언어·활성 번역본을 검증
- RPC가 동의 원장과 UI 캐시를 같은 트랜잭션으로 갱신
- `has_accepted_current_community_policy`가 화면 게이트 상태를 판정
- 게시글·댓글 트리거가 DB에서 최종적으로 작성 권한을 강제

## Slide 5

# 5개 언어를 동등한 참여 경로로

| UI 언어 | 현지화 우선점 | 정책 증빙 |
| --- | --- | --- |
| 한국어 | 기준 원문과 가독성 | canonical SHA-256 |
| English | 직접적이고 짧은 CTA | `en` 원문 SHA-256 |
| 日本語 | CJK 행간·문자 잘림 방지 | `ja` 원문 SHA-256 |
| Español | 긴 문구의 자연스러운 줄바꿈 | `es` 원문 SHA-256 |
| Tiếng Việt | 성조 부호가 안전한 행간 | `vi` 원문 SHA-256 |

## Slide 6

# 온보딩은 안전을 방해가 아닌 안내로 만듭니다

- 1단계: 환영 메시지와 세 가지 참여 원칙 제시
- 2단계: 전체 정책 링크, 필수 체크박스, 버전 표시
- 동의 전에는 “둘러보기만” 허용
- 동의 후에만 게시글·댓글 작성 흐름 활성화

## Slide 7

# 브랜드는 유지하고, 언어별 읽기 부담은 낮춥니다

- 아이보리 배경·다크 올리브 헤더·코랄 CTA로 SokDak 일관성 유지
- 따뜻한 호랑이 마스코트가 안전한 참여를 안내
- 버튼 56dp 이상, 아이콘+문구, 충분한 여백
- 스페인어 확장·일본어 CJK·베트남어 성조를 고려한 유연한 텍스트 영역

## Slide 8

# 운영 배포는 “migration → 정책 게시 → E2E” 순서입니다

- 유지보수 창에서 migration 적용: 테이블·RLS·RPC·게시 트리거 생성
- 즉시 시드 실행: active 정책 1개와 published locale 5개 등록
- 테스트 계정으로 동의 전 거부·동의 후 게시·언어별 원장 기록을 검증
- 24시간 동안 동의 실패·정책 번역 부재·게시 거부를 모니터링

## Slide 9

# 품질 게이트가 출시 전 회귀를 차단합니다

- GitHub Actions: Node 22, `npm ci`, TypeScript 검사
- Expo 프로젝트 상태와 릴리스 환경변수 계약 검사
- migration 파일 비어 있음 방지, 웹 번들 빌드 검증
- 워크플로 파일 반영 후 첫 실행이 모든 통과 조건의 출발점

## Slide 10

# 비공개 베타의 다음 결정

- 정책 전문 5개 언어본 검토·버전 URL·SHA-256 확정
- 운영 DB migration과 시드를 같은 유지보수 창에서 실행
- 새 Android 베타에서 실제 사용자 동의 흐름 테스트
- CI 품질 게이트를 GitHub 웹 UI 또는 `Workflows: write` 권한으로 반영
