{
  "mcpServers": {
    "supabase": {
      "command": "/usr/local/bin/npx",
      "args": ["-y", "@supabase/mcp-server-supabase@latest", "--project-ref=etvrsqfhettkehpltkcp"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
      }
    }
  }
}
