node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
web-build/
.DS_Store
.env
.env*.local
expo-env.d.ts
.vercel

# Local tooling state (Claude Code / claude-flow) — machine-specific, not app code
.claude/settings.local.json
.claude/proven-config.json
.claude/.proven-config-version
.claude-flow/
.venv/
graphify-out/

# Separate nested repo (its own GitHub remote: saruhome/sokdak-landing-page) —
# never add here as an embedded git repository.
sokdak-landing-page/

# 디자인 원본(Figma export). 앱이 실제로 쓰는 에셋은 여기서 추출해
# assets/ 아래에 최적화한 형태로 커밋하므로, 원본 자체는 레포에 넣지 않는다(64MB).
SokDak 화면/
assets/Filter/
assets/Selection/
